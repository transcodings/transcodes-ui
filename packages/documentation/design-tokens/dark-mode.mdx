---
title: 'Dark Mode'
description: '다크 모드 구현 및 테마 전환 방법'
---

## 다크 모드 시스템

Transcodes UI는 **`data-theme="dark"` 속성**을 사용하여 다크 모드를 제어합니다. v0.3.6에서 다크모드 토큰이 **21개에서 58개로 대폭 확장**되었습니다.

### 작동 원리

```css
/* 라이트 모드 (기본) */
:root {
  --ink-black: #1a1a1a;
  --paper-white: #faf9fc;
  --accent-primary: #6b4fd9;
}

/* 다크 모드 */
[data-theme="dark"] {
  --ink-black: #f5f5f5;
  --paper-white: #1a1a1a;
  --accent-primary: #8b6fd9;
}
```

다크 모드 설정 시 CSS 변수가 자동으로 재정의되어 모든 컴포넌트가 즉시 업데이트됩니다.

## 기본 설정

### HTML에서 설정

```html
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Transcodes UI</title>

  <!-- 라이트 모드 CSS (기본) -->
  <link rel="stylesheet" href="@transcodes/design-tokens/build/tokens.css" />
</head>
<body>
  <!-- 라이트 모드 (기본값) -->
  <div id="app">앱 내용</div>

  <!-- 또는 명시적으로 라이트 모드 설정 -->
  <div id="app" data-theme="light">앱 내용</div>

  <!-- 다크 모드 설정 -->
  <div id="app" data-theme="dark">앱 내용</div>
</body>
</html>
```

<Note>
`[data-theme="dark"]` 속성을 **HTML 루트 요소(`<html>`), 본문(`<body>`), 또는 앱 컨테이너 요소**에 설정할 수 있습니다.
</Note>

## JavaScript로 다크 모드 토글

### 간단한 토글

```javascript
// 다크 모드 활성화
document.documentElement.setAttribute('data-theme', 'dark');

// 라이트 모드로 전환
document.documentElement.removeAttribute('data-theme');
// 또는
document.documentElement.setAttribute('data-theme', 'light');

// 현재 테마 확인
const currentTheme = document.documentElement.getAttribute('data-theme');
console.log(currentTheme); // "dark" 또는 null/undefined
```

### 토글 함수

```javascript
function toggleTheme() {
  const html = document.documentElement;
  const isDark = html.getAttribute('data-theme') === 'dark';

  if (isDark) {
    html.removeAttribute('data-theme');
  } else {
    html.setAttribute('data-theme', 'dark');
  }
}

// 사용
document.getElementById('theme-toggle').addEventListener('click', toggleTheme);
```

## localStorage와 연동

사용자의 테마 선택을 **지속적으로 저장**합니다:

<CodeGroup>
  <CodeBlock title="기본 구현">
    ```javascript
    // 테마 저장
    function setTheme(theme) {
      document.documentElement.setAttribute('data-theme', theme);
      localStorage.setItem('theme', theme);
    }

    // 저장된 테마 복원
    function loadTheme() {
      const savedTheme = localStorage.getItem('theme');

      if (savedTheme) {
        document.documentElement.setAttribute('data-theme', savedTheme);
      }
    }

    // 페이지 로드 시 실행
    loadTheme();

    // 토글
    function toggleTheme() {
      const currentTheme = localStorage.getItem('theme') || 'light';
      const newTheme = currentTheme === 'dark' ? 'light' : 'dark';
      setTheme(newTheme);
    }
    ```
  </CodeBlock>
  <CodeBlock title="시스템 선호도 감지">
    ```javascript
    // 사용자의 OS 다크 모드 선호도 감지
    function getSystemTheme() {
      return window.matchMedia('(prefers-color-scheme: dark)').matches
        ? 'dark'
        : 'light';
    }

    // 테마 초기화
    function initializeTheme() {
      const savedTheme = localStorage.getItem('theme');

      if (savedTheme) {
        // 저장된 선택 우선
        document.documentElement.setAttribute('data-theme', savedTheme);
      } else {
        // 시스템 선호도 적용
        const systemTheme = getSystemTheme();
        if (systemTheme === 'dark') {
          document.documentElement.setAttribute('data-theme', 'dark');
        }
      }
    }

    // 시스템 설정 변경 감지
    window.matchMedia('(prefers-color-scheme: dark)').addEventListener(
      'change',
      (e) => {
        if (!localStorage.getItem('theme')) {
          // 사용자가 수동 선택하지 않은 경우만 반응
          document.documentElement.setAttribute(
            'data-theme',
            e.matches ? 'dark' : 'light'
          );
        }
      }
    );

    // 페이지 로드 시 실행
    initializeTheme();
    ```
  </CodeBlock>
  <CodeBlock title="토글 UI">
    ```html
    <button id="theme-toggle" aria-label="테마 전환">
      <span class="theme-icon">🌙</span>
    </button>

    <script>
      const button = document.getElementById('theme-toggle');

      button.addEventListener('click', () => {
        const html = document.documentElement;
        const isDark = html.getAttribute('data-theme') === 'dark';
        const newTheme = isDark ? 'light' : 'dark';

        html.setAttribute('data-theme', newTheme);
        localStorage.setItem('theme', newTheme);

        // 아이콘 업데이트
        button.querySelector('.theme-icon').textContent =
          newTheme === 'dark' ? '☀️' : '🌙';
      });
    </script>
    ```
  </CodeBlock>
</CodeGroup>

## React에서 사용

### Context로 테마 관리

```typescript
import React, { createContext, useContext, useEffect, useState } from 'react';

type Theme = 'light' | 'dark';

interface ThemeContextType {
  theme: Theme;
  toggleTheme: () => void;
  setTheme: (theme: Theme) => void;
}

const ThemeContext = createContext<ThemeContextType | undefined>(undefined);

export function ThemeProvider({ children }: { children: React.ReactNode }) {
  const [theme, setThemeState] = useState<Theme>('light');
  const [mounted, setMounted] = useState(false);

  // 저장된 테마 또는 시스템 선호도 로드
  useEffect(() => {
    const savedTheme = localStorage.getItem('theme') as Theme | null;
    const systemTheme = window.matchMedia('(prefers-color-scheme: dark)').matches
      ? 'dark'
      : 'light';

    const initialTheme = savedTheme || systemTheme;
    setThemeState(initialTheme);
    applyTheme(initialTheme);
    setMounted(true);
  }, []);

  const applyTheme = (newTheme: Theme) => {
    const html = document.documentElement;
    if (newTheme === 'dark') {
      html.setAttribute('data-theme', 'dark');
    } else {
      html.removeAttribute('data-theme');
    }
  };

  const toggleTheme = () => {
    const newTheme = theme === 'dark' ? 'light' : 'dark';
    setTheme(newTheme);
  };

  const setTheme = (newTheme: Theme) => {
    setThemeState(newTheme);
    localStorage.setItem('theme', newTheme);
    applyTheme(newTheme);
  };

  if (!mounted) return <>{children}</>;

  return (
    <ThemeContext.Provider value={{ theme, toggleTheme, setTheme }}>
      {children}
    </ThemeContext.Provider>
  );
}

export function useTheme() {
  const context = useContext(ThemeContext);
  if (!context) {
    throw new Error('useTheme must be used within ThemeProvider');
  }
  return context;
}
```

### 컴포넌트에서 사용

```typescript
import { useTheme } from './ThemeProvider';

export function ThemeToggleButton() {
  const { theme, toggleTheme } = useTheme();

  return (
    <button
      onClick={toggleTheme}
      aria-label="테마 전환"
      className="theme-toggle"
    >
      {theme === 'dark' ? '☀️ Light' : '🌙 Dark'}
    </button>
  );
}

export function App() {
  const { theme } = useTheme();

  return (
    <div>
      <h1>현재 테마: {theme}</h1>
      <ThemeToggleButton />
    </div>
  );
}
```

## v0.3.6 다크모드 토큰 확장

다크모드에서 58개의 토큰이 자동으로 오버라이드됩니다:

### 추가된 토큰 카테고리

| 카테고리 | 토큰 수 | 설명 |
|---------|--------|------|
| **Alpha Primary** | 6개 | `alpha-primary06/12/15/20/30/90` |
| **Alpha Success** | 6개 | `alpha-success06/12/15/20/30/90` |
| **Alpha Warning** | 6개 | `alpha-warning06/12/15/20/30/90` |
| **Alpha Info** | 6개 | `alpha-info06/12/15/20/30/90` |
| **Alpha Ink** | 6개 | `alpha-ink06/12/15/20/30/90` |
| **Alpha onAccent** | 2개 | `alpha-onAccent`, `alpha-onAccentMuted` |
| **Alpha Black** | 3개 | `alpha-black15/30/90` |
| **Brand** | 3개 | `brand-primary`, `brand-primary-hover`, `brand-logo-text` |
| **Overlay** | 5개 | `overlay-shadow-light/subtle/medium/strong/heavy` |
| **Shadow** | 7개 | `shadow-card`, `shadow-button-hover-*`, `shadow-input-focus`, `shadow-modal` 등 |

### Alpha 토큰 다크모드 동작

```css
/* 라이트 모드: accent 색상 기반 반투명 */
:root {
  --alpha-primary12: rgba(107, 79, 217, 0.12);
}

/* 다크 모드: 밝은 색상으로 자동 전환 */
[data-theme="dark"] {
  --alpha-primary12: rgba(139, 111, 217, 0.12);
}
```

### onAccent 토큰

Accent 색상 배경 위에 표시되는 텍스트 색상입니다:

```css
/* 라이트 모드: 흰색 텍스트 */
:root {
  --alpha-onAccent: rgba(255, 255, 255, 0.95);
}

/* 다크 모드: 검정 텍스트 (밝은 accent 배경에서 대비 확보) */
[data-theme="dark"] {
  --alpha-onAccent: rgba(0, 0, 0, 0.95);
}
```

## 색상 토큰 비교

### 주요 색상 변화

다크 모드에서 색상이 어떻게 조정되는지 확인하세요:

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(2, 1fr)', gap: '1rem', marginTop: '1rem' }}>
  <div>
    <h4 style={{ margin: '0 0 0.5rem 0', fontSize: '0.9rem' }}>라이트 모드</h4>
    <div style={{ display: 'flex', flexDirection: 'column', gap: '0.5rem' }}>
      <div style={{ display: 'flex', alignItems: 'center', gap: '0.5rem' }}>
        <div style={{ width: '40px', height: '40px', backgroundColor: '#faf9fc', border: '1px solid #ddd', borderRadius: '4px' }} />
        <span style={{ fontSize: '0.85rem' }}>paper-white</span>
      </div>
      <div style={{ display: 'flex', alignItems: 'center', gap: '0.5rem' }}>
        <div style={{ width: '40px', height: '40px', backgroundColor: '#1a1a1a', borderRadius: '4px' }} />
        <span style={{ fontSize: '0.85rem', color: '#666' }}>ink-black</span>
      </div>
      <div style={{ display: 'flex', alignItems: 'center', gap: '0.5rem' }}>
        <div style={{ width: '40px', height: '40px', backgroundColor: '#6b4fd9', borderRadius: '4px' }} />
        <span style={{ fontSize: '0.85rem', color: '#666' }}>accent-primary</span>
      </div>
    </div>
  </div>
  <div>
    <h4 style={{ margin: '0 0 0.5rem 0', fontSize: '0.9rem' }}>다크 모드</h4>
    <div style={{ display: 'flex', flexDirection: 'column', gap: '0.5rem', backgroundColor: '#1a1a1a', padding: '1rem', borderRadius: '4px' }}>
      <div style={{ display: 'flex', alignItems: 'center', gap: '0.5rem' }}>
        <div style={{ width: '40px', height: '40px', backgroundColor: '#1a1a1a', border: '1px solid #333', borderRadius: '4px' }} />
        <span style={{ fontSize: '0.85rem', color: '#aaa' }}>paper-white</span>
      </div>
      <div style={{ display: 'flex', alignItems: 'center', gap: '0.5rem' }}>
        <div style={{ width: '40px', height: '40px', backgroundColor: '#f5f5f5', borderRadius: '4px' }} />
        <span style={{ fontSize: '0.85rem', color: '#aaa' }}>ink-black</span>
      </div>
      <div style={{ display: 'flex', alignItems: 'center', gap: '0.5rem' }}>
        <div style={{ width: '40px', height: '40px', backgroundColor: '#8b6fd9', borderRadius: '4px' }} />
        <span style={{ fontSize: '0.85rem', color: '#aaa' }}>accent-primary</span>
      </div>
    </div>
  </div>
</div>

## 다크 모드 구현 체크리스트

<Tip>
다크 모드 지원을 위한 확인 항목입니다:
</Tip>

- [x] CSS 임포트: `@transcodes/design-tokens/build/tokens.css`
- [x] HTML에 `data-theme` 속성 설정 메커니즘
- [x] localStorage에 사용자 선택 저장
- [x] 페이지 로드 시 저장된 테마 복원
- [x] 토글 버튼 UI 제공
- [x] 시스템 선호도 감지 (선택사항)
- [x] 모든 페이지에서 테마 일관성 유지
- [x] 접근성: 토글 버튼에 `aria-label` 추가

## 접근성

### WCAG 대비율

다크 모드에서도 **WCAG AA** 기준(4.5:1)을 충족합니다:

```css
/* 다크 모드에서도 충분한 대비율 */
[data-theme="dark"] {
  --ink-black: #f5f5f5; /* #1a1a1a 배경에서 약 15:1 */
  --accent-primary: #8b6fd9; /* #1a1a1a 배경에서 약 5:1 */
}
```

### 모션 선호도 존중

```javascript
// 다크 모드 전환 시 애니메이션 중지
function toggleThemeWithMotionRespect() {
  const prefersNoMotion = window.matchMedia(
    '(prefers-reduced-motion: reduce)'
  ).matches;

  if (!prefersNoMotion) {
    // 애니메이션 적용 가능
    document.documentElement.style.transition = 'background-color 200ms';
  }

  // 테마 전환
  const isDark = document.documentElement.getAttribute('data-theme') === 'dark';
  document.documentElement.setAttribute(
    'data-theme',
    isDark ? 'light' : 'dark'
  );

  // 애니메이션 제거
  setTimeout(() => {
    document.documentElement.style.transition = '';
  }, 200);
}
```

## 일반적인 문제 해결

### 플래시 현상 (Flash of Wrong Theme)

페이지 로드 시 깜박이는 현상을 방지하려면:

```html
<head>
  <script>
    // CSS 로드 전에 실행
    const theme = localStorage.getItem('theme') ||
      (window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');

    if (theme === 'dark') {
      document.documentElement.setAttribute('data-theme', 'dark');
    }
  </script>

  <link rel="stylesheet" href="@transcodes/design-tokens/build/tokens.css" />
</head>
```

### 프레임워크 통합

Next.js, Nuxt 등에서는 미들웨어/플러그인으로 초기 테마를 설정하세요:

```typescript
// Next.js 예시 - app.tsx
import { useEffect } from 'react';

export default function App({ Component, pageProps }) {
  useEffect(() => {
    const theme = localStorage.getItem('theme') ||
      (window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');

    if (theme === 'dark') {
      document.documentElement.setAttribute('data-theme', 'dark');
    }
  }, []);

  return <Component {...pageProps} />;
}
```

## 완전한 예시

<CodeGroup>
  <CodeBlock title="HTML + Vanilla JS">
    ```html
    <!DOCTYPE html>
    <html lang="ko">
    <head>
      <meta charset="UTF-8" />
      <meta name="viewport" content="width=device-width, initial-scale=1.0" />
      <title>다크 모드 예시</title>

      <link rel="stylesheet" href="@transcodes/design-tokens/build/tokens.css" />

      <script>
        // 플래시 방지
        const theme = localStorage.getItem('theme') ||
          (window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');
        if (theme === 'dark') {
          document.documentElement.setAttribute('data-theme', 'dark');
        }
      </script>

      <style>
        body {
          margin: 0;
          padding: 2rem;
          background-color: var(--paper-white);
          color: var(--ink-black);
          transition: background-color 200ms, color 200ms;
        }

        button {
          padding: var(--space-sm) var(--space-md);
          background-color: var(--accent-primary);
          color: white;
          border: none;
          border-radius: 4px;
          cursor: pointer;
        }
      </style>
    </head>
    <body>
      <h1>다크 모드 테스트</h1>
      <button id="toggle">테마 전환</button>

      <script>
        document.getElementById('toggle').addEventListener('click', () => {
          const html = document.documentElement;
          const isDark = html.getAttribute('data-theme') === 'dark';
          const newTheme = isDark ? 'light' : 'dark';

          if (newTheme === 'dark') {
            html.setAttribute('data-theme', 'dark');
          } else {
            html.removeAttribute('data-theme');
          }

          localStorage.setItem('theme', newTheme);
        });
      </script>
    </body>
    </html>
    ```
  </CodeBlock>
</CodeGroup>

## 다음 단계

<CardGroup cols={2}>
  <Card title="Colors" icon="palette" href="/design-tokens/colors">
    다크 모드 색상 변화 확인
  </Card>
  <Card title="Overview" icon="file" href="/design-tokens/overview">
    Design Token 시스템 전체 개요
  </Card>
  <Card title="Guides" icon="book" href="/guides/theming">
    고급 테마 커스터마이제이션
  </Card>
</CardGroup>
